<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/ppp"
    >

    <!-- Semi-transparent Overlay for Readability -->
    <View
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="#66000000" />

    <!-- Content Container -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_centerInParent="true"
        android:orientation="vertical"
        android:gravity="center"
        android:padding="24dp">

        <!-- Header Text -->
        <TextView
            android:id="@+id/welcome_text"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Welcome to Patient Portal"
            android:textSize="28sp"
            android:textColor="@color/primaryColor"
            android:textStyle="bold"
            android:fontFamily="@font/poppins_black"
            android:gravity="center"
            android:layout_marginBottom="16dp" />

        <!-- Descriptive Text -->
        <TextView
            android:id="@+id/description_text"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="A platform where you can easily book appointments, view your medical records, and connect with top healthcare professionals."
            android:textSize="16sp"
            android:textColor="@color/white"
            android:fontFamily="@font/poppins_regular"
            android:gravity="center"
            android:layout_marginBottom="32dp" />

        <!-- Get Started Button -->
        <Button
            android:id="@+id/get_started_button"
            android:layout_width="200dp"
            android:layout_height="wrap_content"
            android:text="Get Started"
            android:textSize="16sp"
            android:textColor="@android:color/white"
            android:backgroundTint="#4CAF50"
            android:background="@drawable/button_background"
            android:layout_marginTop="8dp"
            android:fontFamily="@font/poppins_regular"
            android:layout_gravity="center"
            android:paddingVertical="10dp" />

    </LinearLayout>

</RelativeLayout>
