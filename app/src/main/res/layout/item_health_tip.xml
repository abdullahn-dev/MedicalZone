<?xml version="1.0" encoding="utf-8"?>
<androidx.cardview.widget.CardView xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_margin="16dp"
    app:cardCornerRadius="20dp"
    app:cardElevation="8dp"
    android:backgroundTint="@android:color/white">

    <LinearLayout
        android:orientation="vertical"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:padding="16dp"
        android:background="@android:color/white">

        <!-- Health Tip Header Text -->
        <TextView
            android:id="@+id/healthTipText"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Health Tip"
            android:textSize="16sp"
            android:textColor="@color/primaryColor"
            android:textStyle="bold"
            android:layout_marginBottom="12dp"
            android:letterSpacing="0.5"/>

        <!-- Article Title Text -->
        <TextView
            android:id="@+id/textViewArticleTitle"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Sample Article Title"
            android:textSize="18sp"
            android:textColor="#212121"
            android:layout_marginBottom="16dp"
            android:textStyle="bold"
            android:lineSpacingExtra="4dp"
            android:letterSpacing="0.3"/>

        <!-- Description or Tip Body Text -->
        <TextView
            android:id="@+id/healthTipDescription"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="This is a sample health tip article description. It provides valuable insights about healthy living."
            android:textSize="14sp"
            android:textColor="#616161"
            android:lineSpacingExtra="2dp"
            android:maxLines="3"
            android:ellipsize="end"
            android:layout_marginBottom="16dp"/>

        <!-- Read More Button -->
        <TextView
            android:id="@+id/readMore"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Read More"
            android:textSize="14sp"
            android:textColor="#2196F3"
            android:textStyle="bold"
            android:layout_gravity="start"
            android:layout_marginTop="8dp" />

    </LinearLayout>
</androidx.cardview.widget.CardView>
