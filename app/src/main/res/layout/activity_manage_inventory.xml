<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@android:color/white">

    <!-- TextView added at the top -->
    <TextView
        android:id="@+id/headingTextView"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Manage Inventory"
        android:textSize="32sp"
        android:textColor="@color/primaryColor"
        android:layout_marginTop="16dp"
        android:layout_marginStart="16dp"
        android:layout_marginEnd="16dp"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        android:textStyle="bold"
        android:gravity="center" />

    <!-- RecyclerView to display list of pre-added medicines -->
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/recyclerViewMedicines"
        android:layout_width="0dp"
        android:layout_height="700dp"
        app:layout_constraintTop_toBottomOf="@id/headingTextView"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintBottom_toTopOf="@id/add_inventory_button"
        android:padding="16dp"
        tools:listitem="@layout/item_card_medicine" />

    <!-- Button to add inventory -->
    <Button
        android:id="@+id/add_inventory_button"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:text="Add Inventory"
        android:textColor="@android:color/white"
        android:backgroundTint="@color/primaryColor"
        app:layout_constraintTop_toBottomOf="@id/recyclerViewMedicines"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        android:layout_marginTop="16dp"
        android:layout_marginBottom="16dp"
        android:padding="12dp"
        android:textSize="16sp"
        android:layout_marginHorizontal="32dp"
        android:elevation="4dp" />
</androidx.constraintlayout.widget.ConstraintLayout>
